# Brainstorming session artifacts (browser companion)
.superpowers/

# Rust / Tauri
/target/
/src-tauri/target/
**/*.rs.bk
# Cargo.lock: commit for binary crates (reproducible builds).
# Only ignore a hypothetical workspace-root lock; src-tauri/Cargo.lock is tracked.
/Cargo.lock

# Node / frontend tooling (if added later)
node_modules/
dist/
.vite/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Tauri auto-generated (created by tauri-build at build time)
src-tauri/gen/

# Drift runtime data (should never live in repo)
*.log

# Integration test fixtures (drop real .torrent files here to run the test)
src-tauri/tests/fixtures/*.torrent
